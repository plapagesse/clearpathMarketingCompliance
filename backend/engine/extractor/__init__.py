"""Claim/disclosure extractor package.

Import from the module directly (the package deliberately does not re-export
the `extract` function — it would shadow the `extract` submodule):

    from backend.engine.extractor.extract import extract, ExtractionContext
"""
