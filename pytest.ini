# Wiring for the isolated checker test suite (agent8), whose modules import
# shared builders via `from conftest import ...`: with backend/* being
# packages, a package-local conftest is not importable by that name, so the
# suite's directory joins pythonpath. Not a test edit — merge wiring only.
[pytest]
pythonpath = backend/engine/checker
