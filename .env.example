ANTHROPIC_API_KEY=
# Required only for identity-linked API keys; find it in the Anthropic Console under Settings -> Workspaces
ANTHROPIC_WORKSPACE_ID=
# Enables the demo reset endpoint — never set on a real deployment
CLEARPATH_DEMO=1
